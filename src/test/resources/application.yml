management:
  endpoint.health.show-details: always
  endpoints.web.exposure.include: "*"

spring:
  #    ssl:
#      bundle: kafka1
##      protocol: TLSv1.3
#      protocol: SSL
#    security:
##      protocol: TLSv1.3
#      protocol: SSL
#    properties:
#      ssl.endpoint.identification.algorithm: ''
  output:
    ansi:
      enabled: always
#  ssl.bundle.jks:
#    kafka1:
#      keystore:
#        location: file:etc/ssl/test-client.p12
#        password: password
#        type: PKCS12
#      truststore:
#        location: file:etc/ssl/test-ca.p12
#        password: password
#        type: PKCS12

demo:
  kafka:
    consume-produce:
      enabled: false
      input-topic: consume_produce_input
      output-topic: consume_produce_output
      consumer-lag: 5s
      health:
        enabled: false
        output-topic: health_check
    produce-consume:
      enabled: false
      input-topic: produce_consume_input
      output-topic: produce_consume_output
      consumer-lag: 5s
      health:
        enabled: false
        output-topic: health_check
